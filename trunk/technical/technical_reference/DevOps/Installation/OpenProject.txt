username admin
password 1234567890

https://www.openproject.org/docker/


docker run -d --name openproject  \
    -e EMAIL_DELIVERY_METHOD=smtp \
    -e SMTP_ADDRESS=send.one.com \
    -e SMTP_PORT=587 \
    -e SMTP_DOMAIN=opensoach.com \
    -e SMTP_AUTHENTICATION=login \
    -e SMTP_ENABLE_STARTTLS_AUTO=true \
    -e SMTP_USER_NAME="scrum@opensoach.com" \
    -e SMTP_PASSWORD="mail.scrum" \
    -e SECRET_KEY_BASE=secret \
    -v /opt/tools/openproject/pgdata:/var/lib/postgresql/9.6/main \
    -v /opt/tools/openproject/logs:/var/log/supervisor \
    -v /opt/tools/openproject/static:/var/db/openproject \
    -p 90:80 \
    openproject/community:latest


Upgrade Docker
https://www.openproject.org/operations/upgrading/upgrade-guide-docker/
docker pull openproject/community:latest

docker stop openproject
docker rm openproject

#docker run  as done is previous step


