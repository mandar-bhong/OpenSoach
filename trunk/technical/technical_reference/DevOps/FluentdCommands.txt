https://docs.fluentd.org/v0.12/articles/install-by-rpm

Sending data 
curl -X POST 'http://localhost:8086/db?u=root&p=root' -d '{"name": "test"}'
curl -X POST -d 'json={"json":"message"}' http://localhost:8888/debug.test

$ /etc/init.d/td-agent start
$ /etc/init.d/td-agent stop   -- this command is not working properly need to use restart
$ /etc/init.d/td-agent restart
$ /etc/init.d/td-agent status


log location:
/var/log/td-agent

Configuration:
/etc/td-agent/td-agent.conf

Logs:
logs can be found at path /var/log/td-agent/td-agent.log

Ports Used:
Fluntd by default starts at port :8888 but with integration with influxdb port need to change 


select * from  /.*/